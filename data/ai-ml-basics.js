// AI / ML fundamentals — bite-sized concept refreshers for interviews
// Schema: { id, icon, title, track, difficulty, summary, keyPoints[],
//           formulas[], whenToUse, commonPitfalls[], relatedConcepts[], interviewQs[] }

const aiMlBasics = [

// ============ MATH & STATS ============
{
    id: 1,
    icon: "🎲",
    title: "Probability Fundamentals",
    track: "Math & Stats",
    difficulty: "Easy",
    summary: "Joint, marginal, and conditional probability — the language all of ML is written in.",
    keyPoints: [
        "Joint P(A,B): probability both happen. Marginal P(A) = Σ_B P(A,B). Conditional P(A|B) = P(A,B)/P(B).",
        "Independence: P(A,B) = P(A)·P(B). Conditional independence: P(A,B|C) = P(A|C)·P(B|C) — the foundation of Naive Bayes and graphical models.",
        "Expectation E[X] = Σ x·P(x); Variance Var(X) = E[X²] − E[X]²; Linearity: E[aX+bY] = aE[X]+bE[Y] (always true, even when dependent).",
        "Law of large numbers: sample mean → true mean. Central limit theorem: mean of many samples → Normal, regardless of source distribution. This is why we can use Gaussian assumptions on aggregates.",
    ],
    formulas: [
        "P(A|B) = P(A,B) / P(B)",
        "Var(X) = E[X²] − E[X]²",
        "Cov(X,Y) = E[XY] − E[X]E[Y]",
    ],
    whenToUse: "Every probabilistic model, A/B test analysis, uncertainty quantification, and feature interaction reasoning.",
    commonPitfalls: [
        "Confusing P(A|B) with P(B|A) — the prosecutor's fallacy.",
        "Assuming independence to make math easy when features are correlated → biased estimates.",
        "Forgetting that correlation ≠ causation, and uncorrelated ≠ independent (only true for jointly Gaussian).",
    ],
    relatedConcepts: ["Bayes Theorem", "Information Theory", "Hypothesis Testing"],
    interviewQs: [
        "If 1% of population has a disease and a test is 99% accurate, what's P(sick | positive)?",
        "Why does CLT make A/B testing work even for non-normal metrics?",
    ],
},
{
    id: 2,
    icon: "🧮",
    title: "Bayes Theorem & Bayesian Thinking",
    track: "Math & Stats",
    difficulty: "Easy",
    summary: "Update beliefs from prior to posterior in light of new evidence — the core of probabilistic ML.",
    keyPoints: [
        "Posterior ∝ Likelihood × Prior. P(θ|D) = P(D|θ)P(θ) / P(D).",
        "Maximum Likelihood Estimation (MLE): pick θ that maximizes P(D|θ). Maximum A Posteriori (MAP): pick θ that maximizes P(θ|D) — adds the prior as regularization.",
        "L2 regularization = MAP with Gaussian prior on weights. L1 = MAP with Laplace prior (sparsity).",
        "Naive Bayes assumes feature independence given the class — surprisingly strong baseline for text classification despite the assumption being wrong.",
    ],
    formulas: [
        "P(θ|D) = P(D|θ)·P(θ) / P(D)",
        "log-posterior = log-likelihood + log-prior  (up to constant)",
    ],
    whenToUse: "Spam filtering, A/B test priors, Thompson sampling for bandits, calibration, any low-data scenario where domain priors help.",
    commonPitfalls: [
        "Base rate neglect: ignoring the prior P(θ) when interpreting test results.",
        "Using improper priors that lead to non-integrable posteriors.",
        "Treating MLE point estimates as if they were full posterior — loses uncertainty.",
    ],
    relatedConcepts: ["MLE vs MAP", "Naive Bayes", "Thompson Sampling"],
    interviewQs: [
        "Derive the posterior for a Beta-Binomial conjugate model.",
        "When does MAP equal MLE?",
    ],
},
{
    id: 3,
    icon: "📐",
    title: "Linear Algebra Essentials",
    track: "Math & Stats",
    difficulty: "Easy",
    summary: "Vectors, matrices, dot products, and decompositions — the substrate of every ML model.",
    keyPoints: [
        "Dot product u·v = |u||v|cos θ — measures similarity (cosine similarity = u·v / (||u||·||v||)).",
        "Matrix multiplication = composition of linear transformations. Batch matrix-mul (BMM) is the GPU's bread and butter.",
        "Eigendecomposition: Av = λv. Eigenvectors are directions that don't rotate, eigenvalues are scaling factors. Used in PCA, PageRank, spectral clustering.",
        "Singular Value Decomposition (SVD): A = UΣV^T. Works for any matrix. Top-k SVD = best low-rank approximation (Eckart-Young theorem) → core of recommendation systems.",
        "Rank, null space, and column space tell you what a matrix can and can't do — collinear features = rank-deficient design matrix = unstable regression.",
    ],
    formulas: [
        "cos(u,v) = (u·v) / (||u||·||v||)",
        "PCA: project onto top-k eigenvectors of covariance matrix",
    ],
    whenToUse: "Embeddings (cosine similarity), PCA, matrix factorization for recsys, attention (Q·K^T), transformer math, image kernels.",
    commonPitfalls: [
        "Not normalizing vectors before cosine → magnitude dominates.",
        "Confusing eigendecomposition (square matrices) with SVD (any matrix).",
        "Forgetting that matrix multiplication is not commutative.",
    ],
    relatedConcepts: ["PCA", "SVD", "Cosine Similarity", "Embeddings"],
    interviewQs: [
        "Why is the dot product a similarity measure?",
        "Explain PCA in terms of eigenvectors of the covariance matrix.",
    ],
},
{
    id: 4,
    icon: "📉",
    title: "Gradient Descent & Optimization",
    track: "Math & Stats",
    difficulty: "Medium",
    summary: "How models actually learn: iteratively follow the negative gradient toward lower loss.",
    keyPoints: [
        "Update rule: θ ← θ − η·∇L(θ). Learning rate η is the single most important hyperparameter.",
        "Batch GD (full dataset, slow but stable) vs Stochastic GD (one sample, noisy but fast) vs Mini-batch (the practical compromise, batch sizes 32–8192).",
        "Momentum smooths noisy gradients (exponential moving average of past gradients). Adam = momentum + per-parameter adaptive learning rate (RMSProp). AdamW decouples weight decay from gradient updates — preferred for transformers.",
        "Learning rate schedules: warmup (start small), then decay (cosine, step, or linear). Critical for stable transformer training.",
        "Convex losses (linear/logistic regression) → unique global minimum. Non-convex (deep nets) → local minima, but in high dim most critical points are saddle points, not bad local minima.",
    ],
    formulas: [
        "SGD: θ ← θ − η·∇L(θ; x_i, y_i)",
        "Adam: m_t = β₁m_{t-1} + (1-β₁)g_t ; v_t = β₂v_{t-1} + (1-β₂)g_t² ; θ ← θ − η·m̂_t/(√v̂_t + ε)",
    ],
    whenToUse: "Training literally any parametric model. Choice of optimizer matters: SGD+momentum for vision/CNNs, AdamW for transformers, L-BFGS for small convex problems.",
    commonPitfalls: [
        "Learning rate too high → loss diverges (NaN). Too low → glacial convergence.",
        "Forgetting to scale features → gradient dominated by large-magnitude features.",
        "Not shuffling training data → biased mini-batches → bad convergence.",
        "Using Adam's default LR (1e-3) for transformers without warmup → instability.",
    ],
    relatedConcepts: ["Backpropagation", "Learning Rate Schedules", "Batch Normalization"],
    interviewQs: [
        "Why does Adam often converge faster than SGD but generalize worse?",
        "Explain learning-rate warmup and why transformers need it.",
    ],
},
{
    id: 5,
    icon: "📊",
    title: "Information Theory: Entropy, Cross-Entropy, KL",
    track: "Math & Stats",
    difficulty: "Medium",
    summary: "The math behind classification losses, decision trees, and model comparison.",
    keyPoints: [
        "Entropy H(p) = −Σ p(x) log p(x): average bits needed to encode samples from p. Higher entropy = more uncertainty.",
        "Cross-entropy H(p,q) = −Σ p(x) log q(x): bits needed if you encode using model q for true distribution p. Minimized when q = p.",
        "KL divergence KL(p||q) = H(p,q) − H(p) = Σ p(x) log(p(x)/q(x)). Asymmetric, ≥ 0, = 0 iff p = q.",
        "Cross-entropy loss for classification = log-loss. Minimizing it is equivalent to MLE under categorical likelihood.",
        "Information gain (used in decision tree splits) = H(parent) − weighted H(children). Pick split that reduces entropy the most.",
    ],
    formulas: [
        "H(p) = −Σ p log p",
        "KL(p||q) = Σ p log(p/q)",
        "Cross-entropy loss: L = −Σ y_i log ŷ_i",
    ],
    whenToUse: "Classification loss functions, decision tree splits, variational inference (ELBO), distillation, drift detection (KL between train and prod distributions).",
    commonPitfalls: [
        "KL is not a distance — KL(p||q) ≠ KL(q||p). Use Jensen-Shannon for symmetric.",
        "log(0) = −∞ — always add label smoothing or clip predictions away from 0/1.",
        "Forward vs reverse KL gives different model behavior (mode-seeking vs mean-seeking).",
    ],
    relatedConcepts: ["Cross-Entropy Loss", "Decision Trees", "Knowledge Distillation"],
    interviewQs: [
        "Why is cross-entropy preferred over MSE for classification?",
        "Explain the difference between forward and reverse KL.",
    ],
},

// ============ CLASSICAL ML ============
{
    id: 6,
    icon: "📈",
    title: "Linear & Logistic Regression",
    track: "Classical ML",
    difficulty: "Easy",
    summary: "The two interpretable workhorses every ML practitioner needs to understand cold.",
    keyPoints: [
        "Linear: ŷ = w·x + b, fit by minimizing MSE. Has closed-form solution w = (X^T X)^(-1) X^T y (normal equations) — but you almost always use SGD in practice.",
        "Logistic: ŷ = σ(w·x + b) where σ is sigmoid. Outputs probability for binary classification. Fit by minimizing log-loss (cross-entropy).",
        "Both are linear in the parameters and in the features (logistic is linear in log-odds). To capture non-linearity, manually engineer features (interactions, polynomials, bucketization).",
        "L1 (Lasso) → sparse solutions (feature selection). L2 (Ridge) → small but non-zero weights, more stable. ElasticNet = L1 + L2.",
        "Coefficients are interpretable: in logistic, exp(w_i) is the odds ratio for a one-unit increase in feature i.",
    ],
    formulas: [
        "Linear: w* = (X^T X + λI)^(-1) X^T y  (Ridge closed-form)",
        "Logistic: P(y=1|x) = 1 / (1 + exp(−(w·x + b)))",
    ],
    whenToUse: "Strong baseline for any tabular problem. Production-friendly: fast inference, easy to explain, easy to debug. Required by regulators in finance/healthcare for explainability.",
    commonPitfalls: [
        "Multicollinearity → unstable coefficients (huge variance). Use Ridge or drop correlated features.",
        "Outliers wreck linear regression (squared loss). Use Huber loss or RANSAC.",
        "Imbalanced classes → logistic predicts majority. Use class weights or threshold tuning.",
        "Forgetting to standardize features when using regularization → larger-scale features get penalized less.",
    ],
    relatedConcepts: ["Regularization", "Feature Engineering", "GLMs"],
    interviewQs: [
        "Why use log-loss instead of MSE for logistic regression?",
        "Explain L1 vs L2 regularization geometrically.",
    ],
},
{
    id: 7,
    icon: "🌲",
    title: "Decision Trees & Gradient Boosting (GBDT)",
    track: "Classical ML",
    difficulty: "Medium",
    summary: "The undefeated champions of tabular data: XGBoost, LightGBM, CatBoost.",
    keyPoints: [
        "Decision tree: greedy recursive splits on features, each split chosen to maximize information gain (classification) or reduce variance (regression).",
        "Single trees overfit easily. Two ensembling strategies: Bagging (parallel, e.g. Random Forest — reduces variance) and Boosting (sequential — reduces bias).",
        "Gradient Boosting: each new tree fits the residuals (negative gradient of loss) of the current ensemble. XGBoost adds regularization on tree complexity, second-order gradients (Newton step), and clever histogram-based split finding.",
        "Why GBDT dominates tabular: handles mixed types, monotonic transformations don't matter, no scaling needed, captures interactions automatically, handles missing values natively.",
        "Key hyperparameters: n_estimators (trees), max_depth (3-8 typical), learning_rate (0.01-0.1), min_child_weight, subsample, colsample. Use early stopping on a val set.",
    ],
    formulas: [
        "Boosting: F_m(x) = F_{m-1}(x) + η·h_m(x), where h_m fits −∇L(F_{m-1})",
        "Information gain: IG = H(parent) − Σ (n_child/n)·H(child)",
    ],
    whenToUse: "Default choice for tabular data with structured features (ads ranking, fraud, churn, credit scoring). Beats deep learning on most non-perceptual structured data.",
    commonPitfalls: [
        "Too many trees + high LR → overfit. Prefer many trees + low LR + early stopping.",
        "Trees can't extrapolate beyond training range — predictions are flat outside seen values.",
        "Feature importance from trees is biased toward high-cardinality features. Use permutation importance or SHAP instead.",
        "Categorical features with high cardinality: use target encoding (with leakage protection) or CatBoost's ordered boosting.",
    ],
    relatedConcepts: ["Random Forest", "XGBoost", "SHAP", "Feature Engineering"],
    interviewQs: [
        "Why does gradient boosting overfit less than a single deep tree?",
        "Explain how XGBoost uses second-order gradients.",
    ],
},
{
    id: 8,
    icon: "🎯",
    title: "Bias-Variance & Model Capacity",
    track: "Classical ML",
    difficulty: "Easy",
    summary: "The fundamental tradeoff that explains underfitting, overfitting, and why deep learning is weird.",
    keyPoints: [
        "Total error = Bias² + Variance + Irreducible noise.",
        "High bias = underfitting: model too simple, train and val error both high, gap is small.",
        "High variance = overfitting: model too complex, train error low, val error much higher.",
        "Diagnose with learning curves (error vs training set size) and validation curves (error vs model capacity).",
        "Levers to ↓ bias: bigger model, more features, less regularization, train longer, better architecture.",
        "Levers to ↓ variance: more training data (best), regularization (L1/L2/dropout), simpler model, ensembling, early stopping, data augmentation.",
        "Modern DL paradox: massively overparameterized models often generalize well (double descent phenomenon). The classical U-shape doesn't fully hold for very large models.",
    ],
    formulas: [
        "E[(y − f̂(x))²] = Bias[f̂(x)]² + Var[f̂(x)] + σ²",
    ],
    whenToUse: "Every time you debug a model that isn't performing well — first ask: bias problem or variance problem?",
    commonPitfalls: [
        "Adding more data when you have a bias problem (won't help).",
        "Making the model bigger when you have a variance problem (makes it worse).",
        "Looking only at train accuracy and shipping an overfit model.",
    ],
    relatedConcepts: ["Cross-Validation", "Regularization", "Double Descent"],
    interviewQs: [
        "How do you tell if you have a bias or variance problem from learning curves?",
        "Why might a 100B-parameter model generalize better than a 1B one?",
    ],
},
{
    id: 9,
    icon: "🔀",
    title: "Cross-Validation & Train/Val/Test Splits",
    track: "Classical ML",
    difficulty: "Easy",
    summary: "How to estimate generalization without fooling yourself.",
    keyPoints: [
        "Three-way split: train (fit weights), validation (tune hyperparameters, early stop), test (single final unbiased estimate). Touch test set ONCE.",
        "k-fold CV: split into k folds, train on k-1, validate on 1, rotate. Average the metric. Standard k = 5 or 10. Use when data is limited.",
        "Stratified k-fold: preserves class ratios in each fold — essential for imbalanced classification.",
        "Time-series data: NEVER use random splits. Use forward-chaining (train on past, validate on future). Otherwise you leak future info into training.",
        "Group k-fold: when you have repeated observations of the same entity (user, patient), all rows from the same entity must be in the same fold, or you leak.",
        "Nested CV (outer for evaluation, inner for tuning) gives unbiased performance estimate but is expensive.",
    ],
    formulas: [
        "k-fold estimate: avg metric over k held-out folds",
    ],
    whenToUse: "Always. Especially when you only have a few thousand examples, when classes are imbalanced, or when data has temporal/group structure.",
    commonPitfalls: [
        "Data leakage: fitting scalers/imputers on full dataset before splitting → val set info bleeds into train.",
        "Random shuffling on time-series → looks great offline, fails in production.",
        "Tuning hyperparameters on the test set (effectively making it your val set) → optimistic estimate.",
        "Selecting features using full dataset before CV → leaks target info.",
    ],
    relatedConcepts: ["Bias-Variance", "Hyperparameter Tuning", "Data Leakage"],
    interviewQs: [
        "Why is random k-fold wrong for time-series data?",
        "Explain target leakage with a concrete example.",
    ],
},
{
    id: 10,
    icon: "⚖️",
    title: "Class Imbalance & Calibration",
    track: "Classical ML",
    difficulty: "Medium",
    summary: "Most real-world problems (fraud, churn, harmful content, ad clicks) are 99:1 or worse.",
    keyPoints: [
        "Plain accuracy is useless on imbalanced data — predicting majority gets 99%. Use PR-AUC, recall@high-precision, F1, or expected cost.",
        "Data-level fixes: oversample minority (SMOTE — beware synthetic-leakage in features), undersample majority (faster, sometimes better), class-weighted loss.",
        "Loss-level fixes: class weights in cross-entropy, focal loss (down-weights easy negatives), cost-sensitive learning with explicit cost matrix.",
        "Threshold tuning is often more impactful than fancy losses. Default 0.5 is rarely optimal — pick threshold that maximizes business metric on val set.",
        "Calibration matters separately from ranking. A model can rank well (high AUC) but predict probabilities that don't match actual rates. Use Platt scaling or isotonic regression to calibrate. Critical for ads, insurance, anywhere predicted probability is used downstream.",
        "Boosting and neural nets often produce miscalibrated probabilities; trees and LR tend to be better-calibrated out of the box.",
    ],
    formulas: [
        "Focal loss: FL = −(1−p_t)^γ · log(p_t)",
        "Brier score (calibration): mean((ŷ − y)²)",
    ],
    whenToUse: "Fraud detection, harmful content, medical diagnosis, ad CTR, churn prediction, anomaly detection.",
    commonPitfalls: [
        "Reporting accuracy or ROC-AUC on 99:1 data — both can look great while the model is useless on the minority.",
        "SMOTE on test set or before split → leakage.",
        "Forgetting to recalibrate after threshold or sampling changes.",
    ],
    relatedConcepts: ["PR-AUC", "Focal Loss", "Cost-Sensitive Learning"],
    interviewQs: [
        "Why is PR-AUC better than ROC-AUC for fraud detection?",
        "Explain Platt scaling and when you'd use it.",
    ],
},

// ============ DEEP LEARNING ============
{
    id: 11,
    icon: "🧠",
    title: "Neural Networks & Backpropagation",
    track: "Deep Learning",
    difficulty: "Medium",
    summary: "Stacked linear transformations + nonlinearities + chain rule. That's it.",
    keyPoints: [
        "A neural net is f(x) = σ(W_n · σ(W_{n-1} · ... σ(W_1·x + b_1)) + b_n). Without the nonlinearity σ, no matter how deep, you'd just have a linear model.",
        "Forward pass: compute outputs layer by layer, cache activations. Backward pass: apply chain rule from loss back to each weight. Each weight gets ∂L/∂w.",
        "Universal Approximation Theorem: a 1-hidden-layer net with enough neurons can approximate any continuous function. But depth is exponentially more parameter-efficient than width.",
        "Activation functions: ReLU (default, fast, no vanishing gradient for positive inputs but 'dying ReLU' for negatives), GELU (smoother, used in transformers), Sigmoid/Tanh (legacy, vanishing gradients).",
        "Vanishing/exploding gradients in deep nets: caused by repeated multiplication of Jacobians. Mitigated by ReLU, batch norm, residual connections, careful initialization (He, Xavier).",
        "Initialization matters: zeros → all neurons identical (symmetry). Too-large → exploding activations. Use He init (ReLU) or Xavier/Glorot (tanh/sigmoid).",
    ],
    formulas: [
        "∂L/∂W_l = δ_l · a_{l-1}^T",
        "δ_l = (W_{l+1}^T · δ_{l+1}) ⊙ σ'(z_l)",
    ],
    whenToUse: "Any task with high-dim raw signal (images, text, audio, sensor streams). Less optimal than GBDT for low-dim tabular.",
    commonPitfalls: [
        "Forgetting to zero gradients between batches in PyTorch (`optimizer.zero_grad()`).",
        "Sigmoid/tanh in deep networks → vanishing gradients. Use ReLU/GELU.",
        "Bad initialization → training diverges or never starts learning.",
        "Mixing up batch and sequence dimensions in PyTorch / TF.",
    ],
    relatedConcepts: ["Activations", "Initialization", "Batch Normalization", "Residual Connections"],
    interviewQs: [
        "Derive backprop for a 2-layer network.",
        "Why do we need nonlinear activations?",
    ],
},
{
    id: 12,
    icon: "🛡️",
    title: "Regularization in Deep Learning",
    track: "Deep Learning",
    difficulty: "Medium",
    summary: "Tricks to keep big models from memorizing the training set.",
    keyPoints: [
        "Weight decay (L2): adds λ||w||² to loss. AdamW decouples it from gradient updates — preferred for transformers.",
        "Dropout: randomly zero out activations with probability p during training (typical p = 0.1–0.5). At test time, scale activations by (1−p) or use inverted dropout. Don't use dropout with batch norm in the same block — they conflict.",
        "Batch Normalization: normalize activations to zero mean, unit variance per mini-batch, then learnable scale + shift. Speeds training, reduces sensitivity to initialization, acts as mild regularizer. Doesn't work well with batch size 1 — use LayerNorm or GroupNorm instead.",
        "Layer Normalization: normalizes across features within a single example. Default in transformers — works for any batch size, no train/eval discrepancy.",
        "Data augmentation: domain-specific transformations that preserve labels (image flips, color jitter, mixup, cutout, RandAugment). Often the highest-leverage regularizer.",
        "Early stopping: monitor val loss, stop when it plateaus. Implicit regularizer — limits effective capacity.",
        "Label smoothing: replace one-hot labels with (1−ε) for true class, ε/(K−1) for others. Prevents overconfidence, improves calibration.",
    ],
    formulas: [
        "Weight decay loss: L + λ·||w||²",
        "BN: y = γ·(x−μ_B)/√(σ_B² + ε) + β",
    ],
    whenToUse: "Any deep model, especially when train loss << val loss. Pick augmentation > weight decay > dropout in modern recipes.",
    commonPitfalls: [
        "Using BN in RNNs / very small batches.",
        "Forgetting to disable dropout / switch BN to running stats at eval time (`model.eval()` in PyTorch).",
        "Stacking too many regularizers — model can't learn anything.",
    ],
    relatedConcepts: ["Bias-Variance", "BatchNorm", "AdamW", "Mixup"],
    interviewQs: [
        "Difference between BatchNorm and LayerNorm — why do transformers prefer the latter?",
        "Why does dropout work?",
    ],
},
{
    id: 13,
    icon: "🖼️",
    title: "CNNs (Convolutional Neural Networks)",
    track: "Deep Learning",
    difficulty: "Medium",
    summary: "Built-in translation invariance and locality — why they crushed image tasks for a decade (until ViTs).",
    keyPoints: [
        "Convolution: slide a small filter (e.g. 3×3) over the input, compute dot product at each location → feature map. Same filter weights everywhere → translation equivariance + huge parameter savings vs fully-connected.",
        "Key hyperparameters per conv layer: kernel size (3×3 standard), stride (1 = dense, 2 = downsample), padding (SAME = preserve dim, VALID = shrink), number of filters (channels).",
        "Pooling (max, avg) downsamples spatially → bigger receptive field, fewer params, mild translation invariance. Modern nets often use strided conv instead.",
        "Receptive field grows with depth. Stack many small 3×3 convs > one big conv (more nonlinearity, fewer params).",
        "Architectures: VGG (just stack 3×3), ResNet (residual connections enable 100+ layer training), EfficientNet (compound scaling), Vision Transformers (patches as tokens — different paradigm).",
        "For sequences: 1D conv. For 3D data (video, MRI): 3D conv. Depthwise separable conv (MobileNet) splits spatial and channel mixing — far cheaper.",
    ],
    formulas: [
        "Output size: (W − K + 2P) / S + 1",
        "Params per conv layer: K·K·C_in·C_out + C_out (bias)",
    ],
    whenToUse: "Images (still strong baselines), audio spectrograms, any data with local structure and translation invariance. ViTs surpass CNNs at very large scale; CNNs win at low-data and on edge devices.",
    commonPitfalls: [
        "Forgetting padding → output shrinks layer by layer.",
        "Mismatched input channels (e.g. grayscale vs RGB) — silent shape errors.",
        "Pooling everything away too aggressively — lose fine spatial info needed for detection/segmentation.",
    ],
    relatedConcepts: ["ResNet", "Vision Transformer", "Pooling", "Receptive Field"],
    interviewQs: [
        "Why use 3×3 convs instead of 7×7?",
        "Explain residual connections and why they enable very deep networks.",
    ],
},
{
    id: 14,
    icon: "🔁",
    title: "RNNs, LSTMs, GRUs",
    track: "Deep Learning",
    difficulty: "Medium",
    summary: "Once the standard for sequences. Now mostly replaced by transformers, but still appear in interviews and edge use cases.",
    keyPoints: [
        "Vanilla RNN: h_t = tanh(W_hh·h_{t-1} + W_xh·x_t + b). Same weights at every timestep. In theory captures arbitrary history; in practice suffers vanishing gradients beyond ~20 steps.",
        "LSTM: adds explicit cell state c_t and three gates (forget, input, output) that learn what to remember, write, and read. Solves vanishing gradient via additive cell state path.",
        "GRU: simpler — merges forget and input gates into a single update gate. Fewer params, similar performance to LSTM in most tasks.",
        "Bidirectional RNN: run two RNNs (forward + backward), concat hidden states. Used when full sequence is available (not for streaming).",
        "Training: backprop through time (BPTT) — unroll the recurrence and apply standard backprop. Truncated BPTT for long sequences. Still hard to parallelize across time → slow vs transformers.",
        "Modern alternatives: Transformer (attention, fully parallel across time), State Space Models (Mamba, S4) for very long sequences with linear cost.",
    ],
    formulas: [
        "LSTM gates: f_t = σ(W_f·[h_{t-1}, x_t]); i_t = σ(W_i·[h_{t-1}, x_t]); c_t = f_t⊙c_{t-1} + i_t⊙tanh(W_c·[h_{t-1}, x_t])",
    ],
    whenToUse: "Streaming inputs where you need O(1) memory per step (real-time speech, online sensor data). Small footprint on edge devices. Otherwise use Transformer.",
    commonPitfalls: [
        "Using vanilla RNN for long sequences — vanishing gradient kills it.",
        "Not packing variable-length sequences in PyTorch → wasted compute on padding.",
        "Forgetting to detach hidden state between batches when training stateful RNNs.",
    ],
    relatedConcepts: ["Backprop Through Time", "Transformer", "Sequence Modeling"],
    interviewQs: [
        "How does LSTM solve the vanishing gradient problem?",
        "Why do transformers replace RNNs for most NLP tasks?",
    ],
},

// ============ NLP & LLMs ============
{
    id: 15,
    icon: "🔤",
    title: "Tokenization & Embeddings",
    track: "NLP & LLMs",
    difficulty: "Easy",
    summary: "How text becomes numbers — and why subword tokenization beats words and characters.",
    keyPoints: [
        "Word-level: huge vocab, OOV problem (unseen words at inference). Character-level: tiny vocab, but very long sequences.",
        "Subword (BPE, WordPiece, SentencePiece): learn merges from training data. Frequent words stay whole; rare words split into known pieces. Best of both worlds — used by GPT, BERT, Llama.",
        "BPE: start with characters, iteratively merge most-frequent adjacent pair until vocab size reached.",
        "Embedding: learned dense vector per token (typically 256–4096 dim). Initialized random, learned during training.",
        "Static word embeddings (Word2Vec, GloVe): one vector per word — same for 'bank' (river) and 'bank' (money). Mostly historical now.",
        "Contextual embeddings (BERT, GPT): vector depends on surrounding tokens — different for the two 'bank's. This is what attention enables.",
        "Sentence embeddings: pool token embeddings (mean, [CLS], or trained pooling) → single vector per sentence. Sentence-BERT, E5, BGE produce embeddings tuned for cosine similarity → used in RAG retrieval.",
    ],
    formulas: [
        "Embedding lookup: e_i = E[token_id_i] where E ∈ ℝ^(V × d)",
    ],
    whenToUse: "Every NLP task. Pick a tokenizer that matches your target language coverage. For multilingual: mBERT/XLM-R tokenizers. For code: code-aware tokenizers (CodeBPE).",
    commonPitfalls: [
        "Using a tokenizer different from the model's pretrained one → garbage embeddings.",
        "Counting tokens incorrectly when planning context windows / costs.",
        "Static embeddings for tasks needing word-sense disambiguation.",
    ],
    relatedConcepts: ["BPE", "Word2Vec", "Sentence Embeddings", "RAG"],
    interviewQs: [
        "Why subword tokenization instead of words?",
        "How does Word2Vec's skip-gram objective work?",
    ],
},
{
    id: 16,
    icon: "🎯",
    title: "Transformer & Self-Attention",
    track: "NLP & LLMs",
    difficulty: "Hard",
    summary: "The architecture behind GPT, BERT, T5, Llama — and now vision (ViT), audio (Whisper), and more.",
    keyPoints: [
        "Self-attention: each token computes Query, Key, Value vectors. Attention scores = softmax(QK^T / √d_k). Output = attention · V. Lets every token directly attend to every other token in O(N²) compute.",
        "Multi-head attention: run attention h times in parallel with different learned projections, then concat. Each head can specialize (syntax, coref, position, etc).",
        "Why √d_k scaling: keeps softmax inputs in a sane range so gradients don't vanish for large d_k.",
        "Positional encoding: attention itself is permutation-invariant. Add positional info via sinusoidal (original), learned absolute (BERT), or relative/RoPE (modern LLMs).",
        "Encoder (BERT) vs decoder (GPT) vs encoder-decoder (T5): encoder uses bidirectional attention (sees all tokens), decoder uses causal mask (only past tokens).",
        "Why transformers won: fully parallel across sequence length (vs RNN), captures long-range dependencies directly, scales smoothly with data + parameters (scaling laws).",
        "Cost: O(N²·d) attention is the bottleneck for long contexts. Mitigated by FlashAttention (memory-efficient), sliding window (Mistral), KV cache (reuse past computation), Mixture of Experts.",
    ],
    formulas: [
        "Attention(Q,K,V) = softmax(QK^T / √d_k) · V",
        "Multi-head: concat(head_1, ..., head_h) · W_O",
    ],
    whenToUse: "Any sequence task at any scale you can afford. Vision (ViT), audio (Whisper, AudioLM), RL (Decision Transformer), tabular (TabTransformer).",
    commonPitfalls: [
        "Forgetting the causal mask in a decoder → leaks future tokens during training.",
        "Not scaling QK by √d_k → unstable training.",
        "Mismatched positional embeddings between training and inference (esp. context length extrapolation).",
        "Quadratic memory with sequence length — naïve attention OOMs at long contexts.",
    ],
    relatedConcepts: ["BERT", "GPT", "RoPE", "FlashAttention", "Mixture of Experts"],
    interviewQs: [
        "Walk through self-attention computation for a 3-token sequence.",
        "Why is positional encoding needed?",
        "Compare BERT-style and GPT-style attention masks.",
    ],
},
{
    id: 17,
    icon: "🤖",
    title: "Pretraining, Fine-tuning, RLHF, RAG",
    track: "NLP & LLMs",
    difficulty: "Hard",
    summary: "The four-stage recipe that turns raw text into a useful AI assistant.",
    keyPoints: [
        "1. PRETRAINING: next-token prediction on trillions of tokens of web text. Produces a 'base' model with general world knowledge. Cost: millions of GPU-hours, $10M–$100M+.",
        "2. SUPERVISED FINE-TUNING (SFT): train on (instruction, response) pairs to teach the format of being helpful. Cost: hours-days on existing base model.",
        "3. PREFERENCE TUNING (RLHF / DPO): humans rank pairs of responses; train a reward model; optimize the LLM with PPO (RLHF) or directly with DPO. Aligns with human preferences (helpful, harmless, honest).",
        "4. RETRIEVAL-AUGMENTED GENERATION (RAG): instead of stuffing all knowledge in weights, retrieve relevant docs at inference and put them in the context. Embed corpus → vector DB → query embedding → top-k → stuff into prompt.",
        "When to fine-tune vs prompt vs RAG: Prompt for one-off tasks. RAG for fact-grounded Q&A on private/fresh data. Fine-tune for behavior/style/format that prompting can't reliably enforce. Combine: RAG + light fine-tune is common.",
        "PEFT (LoRA, QLoRA): freeze base model, train tiny adapter matrices. 1000× less GPU memory, 1% of params, often within 1-2% of full fine-tune quality. Default for fine-tuning today.",
        "Evaluation is hard: reference-free metrics (perplexity), reference-based (BLEU, ROUGE — weak), human eval (gold standard but slow), LLM-as-judge (GPT-4 grading), task benchmarks (MMLU, HumanEval).",
    ],
    formulas: [
        "RLHF objective: max E[r(x,y)] − β·KL(π||π_ref)",
        "DPO loss: closed-form alternative to RLHF without a reward model",
    ],
    whenToUse: "Building any LLM-powered product: chatbots, copilots, agents, summarizers. Choose stage based on data and budget.",
    commonPitfalls: [
        "Fine-tuning when prompting + RAG would suffice — wastes time and locks you to a model version.",
        "RAG with bad retrieval → confidently wrong answers (hallucinations).",
        "Catastrophic forgetting during fine-tuning — model loses general abilities. Mitigate with LoRA or replay buffer.",
        "RLHF reward hacking — model finds shortcuts that score well but aren't actually good.",
    ],
    relatedConcepts: ["LoRA / QLoRA", "Vector DB", "Hallucinations", "LLM-as-judge"],
    interviewQs: [
        "When would you choose RAG over fine-tuning?",
        "Explain DPO and why it's simpler than RLHF.",
        "How would you reduce hallucinations in a RAG system?",
    ],
},

// ============ RECOMMENDERS & RANKING ============
{
    id: 18,
    icon: "🎬",
    title: "Recommendation Systems Foundations",
    track: "Recommenders & Ranking",
    difficulty: "Medium",
    summary: "Collaborative filtering, matrix factorization, two-tower — how Netflix/YouTube/TikTok work.",
    keyPoints: [
        "Collaborative Filtering (CF): use user-item interactions only. Two flavors: user-based (find similar users), item-based (find similar items via co-occurrence). Cold-start problem for new users/items.",
        "Content-based: use item features (genre, tags, embeddings). Solves cold-start for new items but not new users. Often a fallback.",
        "Matrix Factorization: factorize user-item matrix R ≈ U · V^T into low-rank user and item embeddings. Trained with ALS or SGD. The classic Netflix Prize approach.",
        "Two-Tower (Dual Encoder): one tower embeds user features (history, demographics) → user vector; another embeds item features → item vector. Score = dot product or cosine. Trained with sampled-softmax or in-batch negatives. Used for retrieval (millions of items) because you can pre-compute item embeddings and use ANN search.",
        "Multi-stage architecture: Retrieval (two-tower / ANN, returns top ~1000) → Coarse Ranking (cheaper model, ~100) → Fine Ranking (heavy model with cross-features, top 10). Trade-off accuracy for latency at each stage.",
        "Negative sampling matters: random negatives are too easy (popular items dominate). Use in-batch negatives, hard negative mining, or impression-based negatives.",
        "Cold-start strategies: content features for new items, demographic-based for new users, exploration (epsilon-greedy, bandits), bootstrapping with most popular.",
    ],
    formulas: [
        "MF objective: min Σ (R_ui − u·v_i)² + λ(||u||² + ||v_i||²)",
        "Two-tower score: s(u, i) = f_user(x_u) · f_item(x_i)",
    ],
    whenToUse: "Any large-scale personalization: video, news, ads, ecommerce, music, friend recs.",
    commonPitfalls: [
        "Optimizing offline metrics (recall@k, NDCG) that don't correlate with online business metrics. Always A/B test.",
        "Popularity bias — model just recommends popular items. Counteract with negative sampling, debiasing, diversification.",
        "Filter bubbles — model converges to user's narrow taste. Inject diversity/exploration.",
        "Position bias in training data — clicked items are higher up. Use position as a feature at train, fix at inference.",
    ],
    relatedConcepts: ["Matrix Factorization", "Two-Tower", "ANN Search (FAISS)", "Learning-to-Rank"],
    interviewQs: [
        "Compare collaborative filtering and content-based recommendation.",
        "Why use a two-tower architecture instead of a single model?",
        "How would you handle cold-start for new users?",
    ],
},
{
    id: 19,
    icon: "🥇",
    title: "Learning-to-Rank (LTR)",
    track: "Recommenders & Ranking",
    difficulty: "Hard",
    summary: "Optimize ordering, not just per-item prediction — the foundation of search and ads.",
    keyPoints: [
        "Three paradigms: Pointwise (predict score per item, sort) — simplest, ignores list structure. Pairwise (learn that item A > item B) — RankNet, LambdaRank. Listwise (optimize the whole ordering) — ListNet, LambdaMART, ApproxNDCG.",
        "Standard offline metrics: NDCG@k (graded relevance, position-discounted), MAP (binary relevance), MRR (single best item). Online: CTR, conversion rate, dwell time, revenue.",
        "LambdaMART (LightGBM/XGBoost rankers) dominated for years and is still the strong baseline for tabular features. Neural rankers (DLRM, DCN, multi-task transformers) win at scale with embedding features.",
        "Multi-task ranking: jointly predict click, conversion, dwell, share. Combine into final score with learned or tuned weights. MMoE (Multi-gate Mixture of Experts) is the standard architecture.",
        "Position bias: top results get clicked more regardless of relevance. Solutions: inverse propensity weighting, position as a feature (drop at inference), pairwise debiasing.",
        "Selection bias: only items the system showed got feedback. Counterfactual evaluation needed for new policies.",
        "Calibration matters when scores are used downstream (e.g. ad auctions need calibrated CTR for billing). Train with isotonic regression or Platt scaling on top.",
    ],
    formulas: [
        "NDCG@k = DCG@k / IDCG@k where DCG = Σ (2^rel_i − 1) / log₂(i+1)",
        "Pairwise loss: L = Σ_{i>j} log(1 + exp(−(s_i − s_j)))",
    ],
    whenToUse: "Search ranking (Google, Amazon), ad ranking (Meta, Google Ads), feed ranking (Twitter, LinkedIn), recommendation ranking stage.",
    commonPitfalls: [
        "Treating ranking as classification — ignores that order matters more than absolute score.",
        "Not handling position bias — model learns 'top = relevant'.",
        "Optimizing single objective when business cares about multiple (engagement vs revenue vs creator fairness).",
        "Selection bias when retraining on logged data → policy collapse.",
    ],
    relatedConcepts: ["LambdaMART", "MMoE", "Position Bias", "Counterfactual Evaluation"],
    interviewQs: [
        "Explain NDCG and why it's better than precision@k.",
        "How do you debias position effects in click data?",
        "Design a multi-task ranker for a video feed.",
    ],
},

// ============ MLOPS & PRODUCTION ============
{
    id: 20,
    icon: "🔧",
    title: "ML in Production: Lifecycle & Pitfalls",
    track: "MLOps & Production",
    difficulty: "Medium",
    summary: "Building a model is 10% of the job. Keeping it healthy in production is the other 90%.",
    keyPoints: [
        "Lifecycle: problem framing → data collection → labeling → feature engineering → training → offline eval → online experiment (A/B) → ramp → monitoring → retraining loop.",
        "Feature store (Feast, Tecton, internal): single source of truth for features, ensures train/serve parity. Solves the #1 production bug: train/serve skew.",
        "Train/serve skew: feature values differ between training pipeline and serving. Causes: different code paths, different data sources, time-of-day effects, missing features at serve time. Mitigate: log production features, replay them in training, run shadow scoring.",
        "Drift: data drift (input distribution changes — e.g. seasonality, new user segment), concept drift (P(y|x) changes — e.g. fraud patterns evolve), label drift (label distribution shifts). Detect with PSI, KL divergence, KS test on features and predictions.",
        "Monitoring: prediction distribution, feature distributions, latency p50/p99, error rate, online metric (CTR, revenue), and offline-online metric correlation. Alert on anomalies.",
        "Retraining cadence: scheduled (daily/weekly), trigger-based (drift detected), continuous (online learning). Always validate new model offline + canary deploy + A/B test before full ramp.",
        "Inference serving: batch (offline scoring, e.g. nightly recs) vs real-time (request/response, <100ms). Serving stack: TF Serving, TorchServe, Triton, BentoML, ONNX Runtime. Optimizations: quantization (int8), distillation, pruning, caching, batching.",
        "Experimentation: A/B test with sufficient power. Watch for novelty effects (early users behave differently) and network effects (treatment leaks to control via shared content).",
        "Reproducibility: pin data version (DVC), code version (git), model artifact, hyperparameters. Use experiment trackers (MLflow, W&B).",
    ],
    formulas: [
        "PSI (Population Stability Index): Σ (p_new − p_old) · ln(p_new/p_old)",
        "Sample size for A/B: n ≈ 16·σ²/δ²  (per arm, for detecting effect δ at 80% power)",
    ],
    whenToUse: "Every production ML system, from a single model to a thousand. The single biggest cause of model failure isn't algorithms — it's pipelines.",
    commonPitfalls: [
        "Train/serve skew — features computed differently in two places. Use a feature store.",
        "Silent label leakage — your 'last_purchase_amount' feature was set after the label event.",
        "No monitoring — model degrades for weeks before someone notices via business metrics.",
        "Retraining on biased logged data — predictions affect what data you collect → feedback loop.",
        "Shipping based on offline metrics alone — they often don't predict online wins.",
        "Not budgeting for the full lifecycle (people often forget monitoring + on-call).",
    ],
    relatedConcepts: ["Feature Store", "Drift Detection", "A/B Testing", "Model Distillation"],
    interviewQs: [
        "How would you detect model drift in production?",
        "Walk me through what happens between a model finishing training and serving its first prediction.",
        "What's train/serve skew and how do you prevent it?",
    ],
},

];
